{
  "name": "top5-cli",
  "version": "0.3.0",
  "description": "CLI for top5 task manager",
  "bin": {
    "top5": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.0.0"
  }
}
